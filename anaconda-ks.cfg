# Generated by Anaconda 38.23.4
# Generated by pykickstart v3.47
#version=F38
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'

# System language
lang pl_PL.UTF-8

# System timezone
timezone Europe/Warsaw --isUtc

# Repo
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-38&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f38&arch=x86_64

%packages
@^minimal-environment
moby-engine
%end

# Network
network --bootproto=dhcp --device=eth0 --onboot=yes --ipv6=auto

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.7.1
ignoredisk --only-use=sda
autopart

# Partition clearing information
clearpart --all --initlabel

#Root password
rootpw --iscrypted --allow-ssh $y$j9T$GGAs4s8BOTbFKR3Ce/nRjPwl$wHFPresTaPqx3W4g1C7AI0FCd5OHmUdqKROOZyvaMP6

# Run post-installation script
%post --erroronfail

cat << 'EOF' > /etc/systemd/system/my-docker.service

usermod -aG docker root
systemctl enable docker

[Unit]
Description=Donwload docker and run
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/bin/docker pull jarczk/devopsy-szalone:latest
ExecStart=/usr/bin/docker run -di -p 8081:8081 --name yarn-app-test jarczk/devopsy-szalone:latest

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable my-docker.service
systemctl start my-docker.service

%end
